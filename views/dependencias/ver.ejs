<%- include("../header"); %>
    <div class="row">
        <div class="col-12">
            <aside class="welcome text-center text-lg-center title-shadow">
                <h1>Dependencias <i class="icon icon-solid icon-dep"></i></h1>
            </aside>
            <% dependencias.forEach(dependencia=> { %>
                <button class="btn btn-primary">
                    <%= dependencia.categorie %>
                </button>
                <% })%>

        </div>
    </div>
    <%- include("../footer"); %>